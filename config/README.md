These files are just for testing and recreating images. 

Will be removed from main branch once project is complete


to use:
Build an image: `./build`
Run container: `./run`
Access container: `./exec`


*All values are hardcoded*